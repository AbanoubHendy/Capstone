import { GetImageUrl } from "../Client/js/PixabayImg"

describe("get the image url from the pixabay api", () => {
    test("Testing the GetImageUrl() function", () => {
           expect(GetImageUrl).toBeDefined();
})});